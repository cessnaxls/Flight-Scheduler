services:
  - type: web
    name: lineforge-dispatch
    runtime: static
    staticPublishPath: .
    buildCommand: echo "No build required"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
